console.log("2">1) //This gives true as first it converts the "2" to numeric 2 and then compares
// We shouldnt compare 2 datatypes 

// Comparisons convert null to a number treating it as 0 therfeore null>0 is false and null>=0 is true
console.log(null>0);
console.log(null==0);
console.log(null>=0);

console.log(undefined == 0)
console.log(undefined > 0)
console.log(undefined <= 0)

console.log("3" === 3) //This will not only check if the numbers are equal but will also check if the datatype is equal 





